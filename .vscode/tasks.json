{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Retrieve from Hubitat",
            "type": "shell",
            "command": "${command:python.interpreterPath} tools/uploader.py MoenFloManager retrieve"
        },
        {
            "label": "Upload to Hubitat",
            "type": "shell",
            "command": "${command:python.interpreterPath} tools/uploader.py MoenFloManager upload"
        }
    ]
}